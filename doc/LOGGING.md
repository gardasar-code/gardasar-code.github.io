# Логирование на устройстве (диагностика BLE)

Как снять отладочный лог Di2 Field с реального Edge. Нужно, например, чтобы
проверить, рекламируется ли Di2 после пробуждения без ручного паринга
(вопрос авто-реконнекта).

## Два канала диагностики

| Канал | Сборка | Кто может | Глубина |
|---|---|---|---|
| **On-screen overlay** (настройка `diagOverlay`) | любая store-сборка | любой пользователь (фото экрана) | агрегат скана + сырой notify-пакет |
| **Файловый лог** (`System.println` → `*.TXT`) | только sideload-diag (`build-diag.sh`) | энтузиаст с USB | полная хронология BLE-событий |

Файл-лог в release **не пишется** (ограничение CIQ — см. «Почему это не просто
включить»). Поэтому для массового онбординга неподдержанных устройств служит
on-screen overlay: он рисуется в `onUpdate` (это не `println`) и работает в
обычной store-сборке.

## On-screen overlay (массовый путь)

Пользователь включает в настройках Connect IQ тоггл **«Diagnostics overlay»**
(`diagOverlay`) → экран целиком отдаётся под диагностику → присылает ФОТО. Без USB,
без создания файлов, без sideload. Основной макет (крупные передачи/батарея) при этом
не рисуется — место занимает максимум технических данных.

Строки diag-экрана (`Di2FieldView.drawDiagScreen`):

| Строка | Значение |
|---|---|
| `● <Phase>` | СВЕТОВОЙ индикатор этапа: цветной кружок (фаза) + слово (Scanning/Connecting/Live/Retry) |
| `dev=N shi=M r=-XX` | устройств в эфире всего / с Shimano-маркером `000018ff` / лучший RSSI |
| `lk=0/1 bat=Y%` | sticky-lock и заряд D-Fly |
| `id=<имя>` | GATT-имя устройства (модель переключателя; в эфире скана недоступно) |
| `mdl=<профиль>` | распознанный профиль серии Di2 (или `?` для неизвестной модели) |
| `R=r/rt F=f/ft R.RR` | распарсенные задняя/передняя передачи + передаточное отношение |
| `len=K pkt=T/G age=S` | длина последнего notify-пакета; пакетов всего/годных за сессию; секунд с последнего |
| `sub=X svc=N rc=R` | состояние подписки CCCD (`ok`, `wr`, `no-svc`, `no-chr`, `no-cccd`, `e<N>`, `ex`); сервисов видно стеку; реконнектов за сессию |
| `reg=<uuid:st> n=<K><f>` | результат `registerProfile` по каждому профилю; K попыток, `d`/`n` — форма с явным CCCD или без |
| `svc: <uuid…>` | какие сервисы стек реально видит на устройстве |
| `<len>: <hex>` ×N | сырые байты notify: последний пакет КАЖДОЙ длины, по 8 байт в строке |

Триаж по `reg`/`sub`/`pkt`:
- `reg=18EF:e<N>` — стек не принял профиль передач; сервис не будет найден вовсе
  (известный баг прошивки, см. `doc/NOTES.md`). Всё остальное ниже — уже следствие.
- `sub=no-svc svc=0` — профиля нет либо GATT-дискавери не завершилась.
- `sub=ok pkt=0/0` — подписка принята, молчит само устройство.
- `pkt=N/0` — пакеты идут, но не той длины: чинить профиль раскладки, не связь.

Триаж по `dev`/`shi`:
- `dev=0` — BLE-стек вообще ничего не видит (Bluetooth выкл / Di2 не вещает).
- `dev>0, shi=0` — в эфире есть BLE, но Di2 **не под нашим маркером** `000018ff`
  (другая серия Di2 → нужно расширять scan-фильтр).
- `shi≥1` — наш маркер виден; смотрим этап/`len`/hex/`id`/`mdl` для разбора коннекта и формата.

## Почему это не «просто включить» (файловый лог)

Connect IQ-приложение **не может создавать произвольные файлы** — единственный
файловый канал это `System.println`, и работает он только при двух условиях:

1. **Debug-сборка** (без флага `-r`). В release/store `System.println` в файл не
   пишется. Но в обычной debug-сборке BLE выключен (`bleEnabled()=false`,
   симулятор BLE нестабилен). Поэтому диагностику собираем спец-скриптом
   `build-diag.sh` — он форсирует BLE on в debug-копии исходников.
2. **Файл лога создан заранее, вручную.** Устройство пишет в
   `GARMIN/APPS/LOGS/<имя_prg>.TXT`, но сам файл не создаёт. Нет файла — вывод
   молча теряется (именно поэтому ранее лог «не появлялся»).

Дополнительно: имя `.txt` должно **точно** совпадать с именем `.prg`; лимит файла
~5 КБ, при заполнении старое уходит в `.BAK`.

## Пошагово

1. Собери диагностический билд:
   ```bash
   ./build-diag.sh        # -> bin/DI2DIAG.prg (+ архивная копия bin/Di2App-<version>-diag.prg)
   ```
   На устройство идёт **DI2DIAG.prg**: имя фиксировано, поэтому лог всегда
   `DI2DIAG.TXT` и создавать новый файл при каждой версии не нужно. Копия с
   версией в имени лежит рядом только для истории сборок.
   В списке полей Edge диаг-сборка называется «… Diag» (у store-версии — «… Beta»)
   и живёт под своим app id, так что обе могут стоять одновременно. ВАЖНО: держать
   на экранах **одну** из них — оба поля регистрируют один и тот же BLE-сервис.
2. **Удали с устройства бета/стор-версию Di2 Field** (иначе сайдлоад дата-филда
   может получить другое внутреннее имя, и матчинг `.txt` сломается).
3. Скопируй `bin/DI2DIAG.prg` в `GARMIN/APPS/` (OpenMTP на macOS).
4. Создай **пустой** файл `GARMIN/APPS/LOGS/DI2DIAG.TXT` (папку `LOGS` при
   необходимости создай сам).
5. Полностью закрой Garmin Express / синхронизацию, отмонтируй устройство.
6. Добавь поле на экран данных, **запиши активность**, прогони сценарий
   (например: подключился → дал Di2 уснуть → покрутил педали без паринга).
7. Подключи устройство, забери `DI2DIAG.TXT` (и `DI2DIAG.BAK`, если есть).

## Формат строк

Каждая строка: `<seq:4> <t_ms> | <сообщение>` (`Di2Log.line`).
`t_ms` — `System.getTimer()`, монотонные миллисекунды от старта (для интервалов).

Ключевые события (источник — `Di2BleDelegate`, флаг `DEBUG`):

| Сообщение | Значение |
|---|---|
| `scan: shimano=N best=… rssi=… lock=…` | результат скана: видим ли Di2 в эфире |
| `pairDevice name=… rssi=…` | попытка поднять соединение |
| `connected` | подписка на notify установлена |
| `notify char=… bytes=[…]` | сырой пакет передач (калибровка) |
| `disconnected` | разрыв связи |
| `reconnect scheduled in N ticks` | запланирован повторный скан |

## Диагностика авто-реконнекта

Смотри строки `scan:` после того, как Di2 «уснул»:

- `shimano≥1` **без** ручного паринга → устройство само рекламируется, проблема
  в нашей логике подключения (чиним кодом).
- `shimano=0` до ручного паринга → D-Fly в спячке не вещает connectable-рекламу;
  ручной паринг неизбежен (ограничение прошивки Di2).

## Перед публикацией в Store

`DEBUG` в `Di2BleDelegate.mc` вернуть в `false` (release-логи не нужны, экономия
кода/ресурсов). Диагностический билд (`build-diag.sh`) и `DI2DIAG.*` в стор не
идут — это только сайдлоад.

## Шаблон ответа пользователю (просьба о дампе новой системы)

Готовый ответ на отзывы/письма вида «добавьте поддержку моей трансмиссии» или
«сделайте бары по передним звёздам». Цель — попросить владельца снять
`diagOverlay` (фото/видео экрана с hex), чтобы расшифровать неизвестный байт
(особенно **передней** звезды для 2x/3x — см. `doc/NOTES.md`).

Отвечаем по-английски (стор), русский держим как эталон. Контакт для отправки
фото/видео **подставить свой** (`sp.lapshin@gmail.com` или ссылку на трекер).

### 🇬🇧 English

> Hi there,
>
> Thank you for the kind words and the suggestion — front-chainring bars are on my list!
>
> So far I've only been able to test the app on my own system, and for exactly these situations I built in a **diagnostic mode** so riders can help me support more setups. The Di2 BLE format isn't documented anywhere, so adding new drivetrains really depends on a bit of help from people who own them. Here's how you can help:
>
> **1) Tell me your drivetrain:**
> - Rear derailleur model (e.g. RD-R8150) and number of rear sprockets (e.g. 12)
> - Front: number of chainrings (1× / 2× / 3×) and tooth counts if you know them (e.g. 50/34)
>
> **2) Turn on the diagnostic mode and capture your shifts:**
> - In Garmin Connect → IQ app settings for Di2 Field, turn on **"Diagnostics overlay"**. The screen will then show raw hex data that helps me decode the gears.
> - Add the field to a data screen and **start recording an activity** (the field only runs while recording).
> - With the bike on a stand, shift through all the gears — front and rear, one click at a time with a short pause on each.
> - Send me either a **photo of the screen at every shift**, or a **video** of the screen while you shift through everything.
> - Turn the overlay back off afterwards for normal use.
>
> With your gear counts plus that hex data I can map it out and add the front display — bar chart included. Thanks for helping make the app better for everyone!
>
> Best regards,
> Sergey Lapshin
> Developer, Di2 Field
> sp.lapshin@gmail.com

### 🇷🇺 Русский (эталон)

> Здравствуйте,
>
> Спасибо за тёплые слова и идею — бары по передним звёздам у меня в планах!
>
> Пока я тестировал приложение только на своей системе, и как раз для таких ситуаций добавил **режим диагностики** — чтобы люди могли помочь с поддержкой других трансмиссий. Формат BLE у Di2 нигде не документирован, поэтому добавление новых систем реально зависит от небольшой помощи их владельцев. Вот как можно помочь:
>
> **1) Сообщите свою трансмиссию:**
> - Модель заднего переключателя (например, RD-R8150) и число задних звёзд (например, 12)
> - Перёд: число звёзд (1× / 2× / 3×) и количество зубьев, если знаете (например, 50/34)
>
> **2) Включите режим диагностики и снимите переключения:**
> - В Garmin Connect → настройки IQ-приложения Di2 Field включите тоггл **«Diagnostics overlay»**. После этого на экране появятся сырые hex-данные, которые помогают мне расшифровать передачи.
> - Добавьте поле на экран данных и **начните запись активности** (поле работает только во время записи).
> - На станке прогоните все передачи — и перёд, и зад, по одному клику с короткой паузой на каждом.
> - Пришлите мне либо **фото экрана на каждом переключении**, либо **видео** экрана, пока переключаете всё подряд.
> - После этого выключите оверлей для обычного использования.
>
> С вашими числами звёзд и этими hex-данными я всё сопоставлю и добавлю отображение переда — с баром в том числе. Спасибо, что помогаете сделать приложение лучше для всех!
>
> С уважением,
> Сергей Лапшин
> Разработчик Di2 Field
> sp.lapshin@gmail.com
